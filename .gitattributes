# 统一换行，避免跨平台克隆（Linux/macOS 评估者）出现整文件 diff
* text=auto

# 明确声明文本类型
*.md   text
*.py   text
*.json text
*.yaml text
*.yml  text
*.txt  text

# 二进制类型（若有）禁止 git 做换行转换
*.png  binary
*.jpg  binary
*.pdf  binary
*.zip  binary

# Windows 批处理必须保持 CRLF，否则 cmd 解析可能出错（双击启动入口）
*.bat   text eol=crlf
